1-->filter() returns a new array containing only the elements that pass the test implemented by the provided callback function.

javascript
Copy code

2-->reduce() applies a callback function to accumulate values of an array into a single result.

javascript
Copy code

3-->Operator	Name	Checks	Example
==	Loose equality	Value only	'5' == 5 → true
===	Strict equality	Value & Type	'5' === 5 → false
Always prefer === to avoid unexpected type coercion.

4-->The spread operator ... is used to:
Expand elements of arrays or objects
Copy or merge arrays/objects
Pass arguments to functions
javascript
Copy code

5-->const obj = {
  name: 'Alice',
  greet() {
    console.log(`Hello, ${this.name}`);
  }
};

obj.greet(); 
